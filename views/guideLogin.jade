extends layout

block content
    h1= title
    form#formLogin(name="login", method="POST", action="/login")
        input#inputGuideEmail(type="text", placeholder="Email", name="email")
        input#inputGuidePassword(type="password", placeholder="Password", name="password")
        button#btnSubmit(type="submit") submit
